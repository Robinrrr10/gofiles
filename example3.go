package main

import "fmt"

func main() {
	/*comments to write*/
	x, y, z := 20, "hihi", true //can initialize multiple variable
	fmt.Println(x, y, z)
	fmt.Printf("Type is x: %T  y: %T z: %T", x, y, z)
}
